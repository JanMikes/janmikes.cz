# see https://www.statie.org/docs/configuration/ for more

imports:
    - { resource: "source/_data/*.yaml" }

parameters:
    site_title: "Jan Mikeš - IT Expert, konzultant, mentor, startup lover"

    generators:
        posts:
            # relative to /source
            layout: '_layouts/post.twig'
